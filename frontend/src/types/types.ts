export type Product = {
  id: number;
  brandName: string;
  categoryName: string;
  price: number;
};

export type Brand = {
  id: number;
  name: string;
};

export type Category = {
  id: number;
  name: string;
};
